fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
python-multipart==0.0.17

# database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# aws bedrock
boto3>=1.34.0
pillow>=10.0.0

# observability (OpenTelemetry tracing)
# - api/sdk: 애플리케이션 내부에서 TracerProvider/BatchSpanProcessor 구성
# - exporter-otlp-proto-http: OTLP HTTP/protobuf 로 dev OTel Collector(:4318) 전송
# - instrumentation-fastapi: inbound HTTP(server span) 자동 계측
# - instrumentation-httpx: backend-auth / backend-book 호출(outbound) 자동 계측
# - instrumentation-botocore: AWS Bedrock Runtime(converse) 호출 자동 계측
# SQLAlchemy instrumentation 은 추가하지 않는다. backend-record 는 CLIAR-123
# 이후 런타임에 DB engine 을 생성/사용하지 않는 OCR 전용 서비스이므로
# 계측할 DB 호출이 존재하지 않는다(app/core/database.py 는 alembic history
# 보존용으로만 유지).
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-httpx==0.65b0
opentelemetry-instrumentation-botocore==0.65b0

# observability (Prometheus HTTP metrics)
# - prometheus-fastapi-instrumentator: FastAPI 요청 계측 + /metrics 노출.
#   infra Prometheus 가 ServiceMonitor 로 스크레이핑한다. Spring(Micrometer)
#   호환 메트릭 http_server_requests_seconds_* 를 app/core/metrics.py 에서 정의한다.
prometheus-client==0.21.1
prometheus-fastapi-instrumentator==7.0.0

# test
pytest==8.3.4
httpx==0.28.0
