apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend-record
  annotations:
    # dev 계정(594532711953)에 만든 Bedrock OCR 용 IAM Role.
    # 아래 ARN 은 IAM Role 을 생성한 뒤(아래 README/명령 참고) 실제 값으로 유지합니다.
    eks.amazonaws.com/role-arn: arn:aws:iam::594532711953:role/dpyb-record-dev-bedrock-ocr
