# ⚠️ prod 배포는 아직 사용하지 않습니다. dev 배포가 안정화되면 아래 주석을 해제하세요.
#
# apiVersion: kustomize.config.k8s.io/v1beta1
# kind: Kustomization
#
# # 상용 환경 (main 브랜치 → dpyb-record 네임스페이스)
# namespace: dpyb-record
#
# resources:
#   - ../../base
#
# # 상용은 2개
# replicas:
#   - name: backend-record
#     count: 2
#
# # 상용용 이미지. CI 가 newTag 를 커밋 SHA 로 갱신합니다.
# images:
#   - name: backend-record
#     newName: 594532711953.dkr.ecr.ap-northeast-2.amazonaws.com/dpyb-dev/dpyb-record
#     newTag: main-latest
#
# # 상용 환경에서 달라지는 설정값만 덮어씀
# patches:
#   - path: configmap-patch.yaml
