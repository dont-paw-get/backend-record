apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend-record
  labels:
    app.kubernetes.io/name: backend-record
  # IRSA: eks.amazonaws.com/role-arn 어노테이션으로 IAM Role 을 연결합니다.
  # 환경마다 계정/Role 이 다르므로 어노테이션은 overlay 에서 주입합니다.
  # (dev: k8s/overlays/dev/serviceaccount-patch.yaml)
